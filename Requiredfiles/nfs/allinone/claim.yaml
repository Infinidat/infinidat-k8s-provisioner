kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nfs
  annotations:
    volume.beta.kubernetes.io/storage-class: "infinidat-nfs-rw"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi

